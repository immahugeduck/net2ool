<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Compose supplies the real theming; this exists so the manifest has a
      valid android:theme and the launch window matches the dark UI instead of
      flashing white.
    -->
    <style name="Theme.Net2oolAgent" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">#0B0E11</item>
        <item name="android:statusBarColor">#0B0E11</item>
        <item name="android:navigationBarColor">#0B0E11</item>
    </style>
</resources>
